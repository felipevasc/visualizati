import { Breakpoints, Offsets, Spacing } from '../types/layout';

type ClassNames = {
    [key: string]: boolean;
};
export declare const getBreakpoints: ({ sm, md, lg, xl }: Breakpoints) => ClassNames;
export declare const getOffsets: (offsets: Offsets) => ClassNames;
export declare const getMargin: (value?: Spacing) => ClassNames;
export declare const getPadding: (value?: Spacing) => ClassNames;
export declare const getStyle: (prefix: string, value?: number | string) => string;
export {};
