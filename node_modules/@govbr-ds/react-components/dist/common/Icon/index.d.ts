import { default as IMtProps } from '../../types/IMtProps';
import { IconSize } from '../../types/icons';

type IconProps = {
    icon: string;
    size?: IconSize;
    badge?: React.ReactNode;
} & IMtProps;
declare const Icon: ({ badge, ...rest }: IconProps) => import("react/jsx-runtime").JSX.Element;
export default Icon;
