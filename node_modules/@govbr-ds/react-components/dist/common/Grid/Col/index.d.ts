import { AlignSelf, Breakpoints, Offsets, Spacing } from '../../../types/layout';

export type ColProps = {
    children?: React.ReactNode;
    className?: string;
    alignSelf?: AlignSelf;
    margin?: Spacing;
    padding?: Spacing;
};
type Props = ColProps & Breakpoints & Offsets;
declare const Col: (props: Props) => import("react/jsx-runtime").JSX.Element;
export default Col;
