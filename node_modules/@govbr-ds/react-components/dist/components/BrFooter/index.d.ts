import { default as IMtProps } from '../../types/IMtProps';
import { ColSize } from '../../types/layout';
import { default as React } from 'react';

export interface BrFooterProps extends React.HTMLAttributes<HTMLDivElement>, IMtProps {
    /** URL da Logo */
    urlLogo?: string;
    /** Links do footer */
    links?: Array<{
        category: string;
        size?: ColSize;
        items: Array<{
            label: string;
            link: string;
        }>;
    }>;
    /** Redes sociais do footer. */
    socialNetworks?: Array<{
        icon: string;
        link: string;
        name: string;
    }>;
    /** Texto ao fim do footer, sobre a licença ou termos de uso. */
    userLicenseText?: React.ReactNode;
    /** Imagens no fim do footer. */
    footerImages?: Array<{
        url: string;
        link: string;
        name: string;
    }>;
    /** Tema invertido */
    inverted?: boolean;
}
declare const BrFooter: React.ForwardRefExoticComponent<BrFooterProps & React.RefAttributes<HTMLDivElement>>;
export default BrFooter;
