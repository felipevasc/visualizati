import { Breakpoints } from '../../types/layout';
import { default as React } from 'react';
import { default as IMtProps } from '../../types/IMtProps';
import { default as IMenuItem } from './IMenuItem';
import { default as IMenuLink } from './IMenuLink';
import { default as IMenuLogo } from './IMenuLogo';
import { default as ISocialNetwork } from './ISocialNetwork';

export interface MenuProps extends React.HTMLAttributes<HTMLDivElement>, IMtProps, Omit<Breakpoints, "xs"> {
    systemLogoUrl?: string;
    systemName?: string;
    contextualTriggerTitle?: string;
    data: IMenuItem[];
    logos?: IMenuLogo[];
    externalLinks?: IMenuLink[];
    socialNetworks?: ISocialNetwork[];
    info?: React.ReactNode;
    type?: "normal" | "push" | "contextual";
    startActive?: boolean;
    shadow?: boolean;
    closable?: boolean;
    density?: "small" | "normal" | "large";
}
export interface MenuRef extends HTMLDivElement {
    element: HTMLDivElement;
}
declare const BrMenu: React.ForwardRefExoticComponent<MenuProps & React.RefAttributes<MenuRef>>;
export default BrMenu;
