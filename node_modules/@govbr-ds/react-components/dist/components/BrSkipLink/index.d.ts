import { default as React } from 'react';
import { default as IMtProps } from '../../types/IMtProps';
import { default as ISkipLink } from './ISkipLink';

export interface BrSkipLinkProps extends React.HTMLAttributes<HTMLElement>, IMtProps {
    /** Lista de elementos do skiplink. */
    data: ISkipLink[];
    /** Se é do tipo composto. */
    full?: boolean;
}
declare const BrSkipLink: React.ForwardRefExoticComponent<BrSkipLinkProps & React.RefAttributes<HTMLElement>>;
export default BrSkipLink;
