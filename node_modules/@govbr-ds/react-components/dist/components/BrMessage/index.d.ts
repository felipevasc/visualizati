import { default as IMtProps } from '../../types/IMtProps';
import { MessageStatus } from '../../types/messages';
import { ReactNode } from 'react';

export interface BrMessageProps extends React.HTMLAttributes<HTMLElement>, IMtProps {
    id?: string;
    status?: MessageStatus;
    category?: "feedback" | "message";
    message: ReactNode;
    title?: string;
    inlineTitle?: boolean;
    closable?: boolean;
}
declare const BrMessage: React.ForwardRefExoticComponent<BrMessageProps & React.RefAttributes<HTMLElement>>;
export default BrMessage;
