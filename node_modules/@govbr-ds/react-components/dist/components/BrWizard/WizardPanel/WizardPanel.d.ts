import { default as React } from 'react';
import { default as IMtProps } from '../../../types/IMtProps';

export interface WizardPanelProps extends React.HTMLAttributes<HTMLDivElement>, IMtProps {
    /** Título do painel */
    title: string;
    /** Se mostra ou não o header do título */
    showHeader?: boolean;
    /** Se o painel está desabilitado */
    disabled?: boolean;
}
declare const BrWizardPanel: React.ForwardRefExoticComponent<WizardPanelProps & React.RefAttributes<HTMLDivElement>>;
export default BrWizardPanel;
